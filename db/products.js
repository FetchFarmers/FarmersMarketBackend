// getProducts(): Retrieves a list of all products in the database.

// getProductById(id): Retrieves a single product by its ID.

// getProductsByCategory(category): Retrieves a list of products that belong to a particular category.

// getProductsBySubcategory(subcategory): Retrieves a list of products that belong to a particular subcategory.

// createProduct(productData): Creates a new product with the given data.

// updateProduct(id, updates): Updates an existing product with new data.

// deleteProduct(id): Deletes a product from the database by its ID.